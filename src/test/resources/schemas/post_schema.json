{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSONPlaceholder Post",
  "description": "Validates a single post object from https://jsonplaceholder.typicode.com/posts/{id}",
  "type": "object",
  "required": ["userId", "id", "title", "body"],
  "properties": {
    "userId": {
      "type": "integer",
      "minimum": 1,
      "description": "ID of the user who created this post"
    },
    "id": {
      "type": "integer",
      "minimum": 1,
      "description": "Unique post identifier"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Post title — must not be empty"
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Post body text — must not be empty"
    }
  },
  "additionalProperties": false
}
